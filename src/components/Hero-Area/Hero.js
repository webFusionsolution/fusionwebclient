import "./Hero.css";
import Slider from "../SliderSection/Slider";
import React from "react";
const Hero = (props) => {
  return (
    <div className="hero_area">
      <Slider/>
    </div>
  );
};

export default Hero;
